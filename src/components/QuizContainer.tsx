import { IChildren } from "../interfaces/defaultInterfaces";

export const QuizContainer = ({ children }: IChildren) => (
  <div className="col-12 p-0 col-xl-8 mb-5 QuizContainer">{children}</div>
);
